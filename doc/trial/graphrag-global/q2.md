*This is a model's reading of a model's extraction of the tree — testimony, not evidence.  `card:graphrag-c.md`.*

**Question:** What are the method's neighbours — the prior art each of the tree's mechanisms already has a name in?

*keywords (claude-haiku-4-5-20251001): low ['tree', 'mechanisms', 'methods', 'prior art']; high ['prior art', 'naming conventions', 'algorithmic foundations', 'related work', 'mechanism design']*  
*retrieved: 514 entities matched + 1550 by one hop, 4225 relations, 40000 chars (cut); answer by claude-sonnet-5; 15294 in, 1277 out, $0.064 at assumed prices*

Based on the context, "the tree's mechanisms" refers to `smaller-models-and-the-tree`'s three named jobs — structural rules, judgment norms, and initiative — and the neighbouring entity that identifies prior art for these is `mechanism-not-instructions`.

## The tree's three jobs

The memory document `smaller-models-and-the-tree` frames the tree as having three separable jobs — structural rules, judgment norms, and initiative — each of which "degrade separately on smaller models," measured on 9B and 1B models (`doc/memory/smaller-models-and-the-tree.md`). The `doc/memory/README.md` description of this same document says "structural rules survive, judgment norms go first, initiative goes furthest," confirming the three-part structure, and adds that a third model "conditioned by words alone" had its transcript answer "the seam question" on 2026-08-25 (`doc/memory/README.md`).

## Where prior art is named

The entity most directly answering "what name does each mechanism already have in prior art" is `mechanism-not-instructions`. Its `doc/memory/README.md` description states: "the idea is widely converged on, but rules held by a suite are uncommon, so a copy takes the mechanisms and not the prose" — implying that the general idea (something like rules/judgment/initiative as a structure) is already recognized elsewhere, but the specific enforcement-by-suite implementation is not (`doc/memory/README.md`). A second description of the same document, from `doc/notes/notes-on-the-find.md`, dates it to 2026-08-19 and identifies "four uncommon properties: rules held by a suite, claims citing their paid-for defect, an author/session contract, and a metabolism" — these four properties appear to be the tree's actual mechanisms, each set against a backdrop of otherwise-common prior art (`doc/notes/notes-on-the-find.md`). The `doc/memory/the-evaluation-loop.md` entry additionally calls `mechanism-not-instructions` "a related memory concept referenced in the document," without further detail on the naming (`doc/memory/the-evaluation-loop.md`).

## Adjacent naming candidates

Other neighbouring entities gesture toward named precedents without fully spelling out a one-to-one mapping in the context given:

- `weights-context-suite` distinguishes "weights for what the model must know, context for what it must currently obey, suite for what must be guaranteed," a triad that parallels the tree's three jobs but is presented as the project's own vocabulary rather than an explicit external prior-art name (`doc/memory/README.md`).
- `henri-working-style` and `henri-prior-tools` document Henri's own earlier tools (oscillseq, mide, xylem) as prior art for specific mechanisms, but these are framed as personal precedent rather than named external art (`doc/memory/henri-prior-tools.md`, `card:gui-is-difficult.md`).
- `capacity-is-not-a-caller` states "there is one keeper with three legitimate pulls," another three-part structure adjacent to the tree's jobs, again without an explicit external prior-art label (`doc/memory/README.md`).

## What the context does not carry

The context does not provide an explicit table or list mapping each of the tree's three named mechanisms (structural rules, judgment norms, initiative) to a specific external prior-art term or discipline name (e.g., "this is called X in field Y"). The closest material — `mechanism-not-instructions` — asserts convergence with "the idea" broadly but does not, in what is given here, name what that prior art is called (`doc/memory/README.md`, `doc/notes/notes-on-the-find.md`).

---
*citations: 6 resolve, 0 do not; 2 sentence(s) with no citation*

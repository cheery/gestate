*This is a model's reading of a model's extraction of the tree — testimony, not evidence.  `card:graphrag-c.md`.*

**Question:** What has Henri found in this project, if anything — the working method and the whole project — and is it unique?

*keywords (claude-haiku-4-5-20251001): low ['Henri']; high ['working method', 'project overview', 'uniqueness', 'findings', 'authorship']*  
*retrieved: 312 entities matched + 808 by one hop, 2187 relations (0 in the context); ranking docs; 40000 chars (cut); answer by claude-sonnet-5; 15297 in, 931 out, $0.059 at assumed prices*

Based on the context, this is difficult to answer with confidence.

## What the context says about "finding"

The context contains entities named **`doc/notes/notes-on-the-find.md`** and related material (e.g., `mechanism-not-instructions`), which reference "the find" — but the actual content of what Henri is described as having found is not laid out as a single clear claim in the material provided. What is present are fragments that gesture at findings *within* the project rather than a finding *about* the project as a whole:

- Henri is repeatedly credited with specific, local discoveries and resolutions — for example, he "reported and verified various defects including F106 and F110" and "answered F113 design question and refined warning behavior" (`fixme.md`). These are findings of individual defects, not a finding about the method or the project overall.
- A document titled `mechanism-not-instructions` states that "the idea is widely converged on, but rules held by a suite are uncommon, so a copy takes the mechanisms and not the prose" (`doc/memory/README.md`), and a related note from `doc/notes/notes-on-the-find.md` identifies "four uncommon properties" of the project — rules held by a suite, claims citing their paid-for defect, an author/session contract, and a metabolism. This is the closest thing in the context to a claim about the project's *working method* being unusual, but it is attributed to the document itself, not explicitly stated as "Henri's finding."

## On uniqueness

The `mechanism-not-instructions` material does address uniqueness directly, but ambiguously: it says the underlying *idea* is "widely converged on" (i.e., not unique), while the specific combination of properties — "rules held by a suite" being "uncommon" — is treated as unusual (`doc/memory/README.md`; `doc/notes/notes-on-the-find.md`). So according to this thread, the general concept is not unique, but the concrete implementation (mechanisms over prose, suite-enforced rules, defect-citation, author/session contract, metabolism) is described as uncommon.

## What's missing

The context does not contain a direct statement attributing this "finding" to Henri personally, nor a clear passage where Henri himself concludes the project or its method is (or isn't) unique. The entity list shows Henri active mainly as a reporter/verifier of defects and a designer answering specific questions (`fixme.md`), and separately as the source of a documented "working style" (`doc/memory/README.md`, `henri-working-style`), but no excerpt here ties him explicitly to a verdict on the project's uniqueness. Answering more precisely would require content from `doc/notes/notes-on-the-find.md` itself, which is only referenced secondhand in this context rather than quoted at length.

---
*citations: 3 resolve, 0 do not; 6 sentence(s) with no citation*

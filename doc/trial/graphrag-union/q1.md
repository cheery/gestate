*This is a model's reading of a model's extraction of the tree — testimony, not evidence.  `card:graphrag-c.md`.*

**Question:** What keeps a keeper in check — what stops the person who keeps this tree from drifting, and what would spread the method?

*keywords (claude-haiku-4-5-20251001, union): low ['keeper', 'the tree', 'the-keeper-is-the-qualification', 'the-keepers-evening', 'the-tree-meets-people-on-pull', 'gestate-house-rules-authorship', 'gestate-andon', 'gestate-blind-model-test']; high ['authority', 'accountability', 'methodology', 'collaboration', 'validation', 'working method', 'defect discovery']; 5 of 7 high keywords are tags in the store*  
*retrieved: 1059 entities matched + 1464 by one hop, 5497 relations (133 in the context); ranking themes, hop from all; 40002 chars (cut); answer by claude-sonnet-5; 17215 in, 1348 out, $0.067 at assumed prices*

# What Keeps a Keeper in Check

## Internal checks: qualification through doubt, not certainty

The tree's own doctrine holds that uncertainty is not a disqualification for the keeper but the qualification itself — a memory established from "the fire conversations" and recorded in `doc/memory/the-keeper-is-the-qualification.md`, echoed in `journal/2026-08.md` and `doc/testimony.md`. This reframes drift-anxiety: a keeper who doubts is doing the job correctly, not failing at it.

## Structural checks: the fire, the ledger, and standard work

The primary mechanism against drift is **the keepers evening** — a weekly session, adopted 2026-08-21, whose standard work is defined in `keeper.md`: read the lamps, open the decisions batch, measure one rule, pass over the pile, rotate monthly (`doc/memory/README.md`). This is explicitly framed as a resource-limited discipline — "the keeper's limited attention" — in `doc/memory/capacity-is-not-a-caller.md`.

Underneath that sits **the fire**, where humans review the ledger's dated records and "hold the process," per `doc/notes/notes-on-corrections.md`. Overrides and corrections are meant to land there as dated lines, giving the review something concrete to check against rather than trusting memory or intention.

## External checks: outside critique and stranger tests

Henri himself is checked by an outside interlocutor: **Fable** provides "structured critique and guidance to Henri across three exchanges, acting as a check on his decision-making and drift patterns" (`doc/notes/notes-on-drift.md`). The same document is explicitly titled around the question "What keeps the keeper honest," addressing Henri's own "self-observation of drifting" and exploring accountability mechanisms (`doc/notes/notes-on-drift.md`).

Separately, the tree insists that self-assessment is suspect: `doc/memory/the-evaluation-loop.md` declares that "a session judging this method is a product of it," so "the real check should be routed to a stranger" (`doc/memory/README.md`). This is enacted concretely through devices like **card:stranger-test.md**, which brings in a person who does not read code for a living to test whether the system actually works as claimed, rather than as the keeper believes (`fixme.md`).

## What spreads the method

The clearest evidence of the method spreading is its use on a second project: on 2026-08-24 "the audit was first run against `~/tend`, a second project started under this method" (`doc/method.md`). The blind, multi-model tests — clones rather than worktrees, keys kept out of the shared parent — demonstrate the method being ported across different model substrates entirely, including smaller models, as documented in `gestate-blind-model-test` (`doc/memory/README.md`, `doc/memory/smaller-models-and-the-tree.md`). Additionally, `doc/method.md` is described as "the visitors' front" — a one-page overview meant to let outsiders understand how the project runs without needing the full depth of the tree (`README.md`, `journal/2026-08.md`), functioning as the propagation surface for anyone approaching from outside.

## Summary

In short, the context describes a layered set of checks — doctrinal (doubt as qualification), procedural (weekly keeper's evening and the fire/ledger review), and external (Fable's critique, stranger tests, and the refusal to let the method judge itself) — while spread is evidenced by the method's transplant onto a second project (`~/tend`) and its explicit visitor-facing document (`doc/method.md`).

---
*citations: 13 resolve, 1 do not — ~/tend; 2 sentence(s) with no citation*

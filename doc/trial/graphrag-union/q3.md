*This is a model's reading of a model's extraction of the tree — testimony, not evidence.  `card:graphrag-c.md`.*

**Question:** Why did the reviewing guest's reviews work, and how would one keep a reviewer in the project?

*keywords (claude-haiku-4-5-20251001, union): low ['reviewing guest', 'reviews', 'reviewer', 'project']; high ['authorship', 'collaboration', 'defect discovery', 'validation', 'retention']; 4 of 5 high keywords are tags in the store*  
*retrieved: 996 entities matched + 1421 by one hop, 5107 relations (135 in the context); ranking themes, hop from all; 40002 chars (cut); answer by claude-sonnet-5; 17170 in, 1639 out, $0.072 at assumed prices*

# Why the Guest's Reviews Worked

The context points to a specific quality: the reviewer was outside the tree. `doc/notes/notes-on-the-find.md` describes **notes-on-reviews.md** as recording that "a guest reviewer outside the tree found a missing decision invisible from inside" — the value of the review came precisely from not being embedded in the project's accumulated assumptions.

`doc/notes/README.md` characterizes the document itself as "a conversation about guest reviews, role versus tool, fresh context, and audit limitations," which suggests the mechanism behind the guest's effectiveness was fresh context — a session without the history, habits, and blind spots that build up inside a long-running tree — combined with a deliberate framing of "role versus tool" (the guest was acting as a reviewer, not merely being queried as an instrument).

This fresh-context review process was substantial rather than a single pass: `spec/roles/README.md` describes the reviewer role as originating from "thirteen reviews of the project's artifacts, initiated by Henri on 2026-09-08," and `doc/instruments.md` dates the reviewer role's origin to a document from that same day. So the "guest" here was not one review but a repeated practice of bringing in an outside, unindebted perspective across many passes, which is what surfaced things — like the missing decision — that someone working from inside could not see.

# How to Keep a Reviewer in the Project

The context shows the answer was to formalize the guest's function as a standing **role** rather than treat it as a one-off visit. `spec/roles/reviewer.md` defines this role concretely: "a session role that reads artifacts and reports what is solid, missing, or problematic without writing code or touching the tree." That is, the role is kept in the project by codifying its boundaries — it observes and reports, and it explicitly does not write code or touch the tree — so that any session can be assigned this role and reproduce the guest's outside-perspective value without that session becoming entangled in authorship or drift.

The thirteen prior reviews (`spec/roles/README.md`) served as the empirical basis from which this role was drafted, implying that the way to retain a reviewer's usefulness is to distill repeated guest sessions into a written, reusable specification rather than relying on informal, one-time guest visits.

Beyond this — the specifics of onboarding, incentivizing, or otherwise retaining a *particular* reviewer as a continuing collaborator — the context does not say; it only documents how the reviewer's *function* was captured as a defined role in `spec/roles/reviewer.md` and `spec/roles/README.md`.

---
*citations: 5 resolve, 0 do not; 3 sentence(s) with no citation*

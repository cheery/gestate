*This is a model's reading of a model's extraction of the tree — testimony, not evidence.  `card:graphrag-c.md`.*

**Question:** What is this tree?

*keywords (claude-haiku-4-5-20251001, vocab): low ['the tree']; high ['definition', 'reference']; 2 of 2 high keywords are tags in the store*  
*retrieved: 444 entities matched + 1191 by one hop, 2798 relations (141 in the context); ranking themes-entities, hop from all; 40002 chars (cut); answer by claude-sonnet-5; 17035 in, 1461 out, $0.070 at assumed prices*

## The Tree

Across the context, "the tree" appears as a single concept with three distinct but related descriptions, each drawn from a different document, and they do not agree on emphasis.

**As a teaching system.** The journal describes the tree as "a living system of documented methods, rules, and journal entries designed to teach its keeper and visitors through written practice" (`journal/2026-08.md`). This framing treats the tree as pedagogical: it is not just a repository of rules but an active mechanism that instructs the people who read and write to it, including whoever is designated its "keeper."

**As a workplace for memoryless workers.** A note on "the find" describes the tree instead as "an append-only system that conditions the next session's behavior through deterministic rules, gates, and a consent register, functioning as a workplace for workers who do not remember" (`doc/notes/notes-on-the-find.md`). Here the emphasis shifts from teaching to conditioning: the tree exists because the sessions that work on it have no persistent memory of their own, so the tree's rules, gates, and consent mechanisms take over the role that memory would otherwise play, shaping what each new, forgetful "worker" (a session) does next.

**As cached deliberation.** A note on cues gives a third description: the tree is "an external, cached slow thinking system that stores the residue of past deliberation and conditions future fast passes" (`doc/notes/notes-on-cues.md`). This framing casts the tree in terms borrowed from dual-process reasoning — slow, deliberate thought is done once and its residue is cached in the tree, so that later, faster passes (presumably by sessions or the keeper) can draw on that stored deliberation rather than re-deriving it.

**How these descriptions relate.** The three descriptions are not contradictory so much as three angles on the same object: a documented, append-only structure of methods, rules, journal entries, gates, and a consent register that persists across sessions which themselves do not persist. The teaching framing (`journal/2026-08.md`) stresses the tree's effect on people (keeper and visitors); the workplace framing (`doc/notes/notes-on-the-find.md`) stresses its effect on memoryless sessions via deterministic conditioning; and the cached-deliberation framing (`doc/notes/notes-on-cues.md`) stresses the mechanism by which that conditioning works — stored residue of past slow thinking substituting for thinking that would otherwise have to be redone.

**Context from surrounding entities.** The broader context describes the tree's constituent parts in ways consistent with this picture: `board/README.md` and `AGENTS.md`/`CLAUDE.md` describe board and README files that "should be read before beginning work," `fixme.md` and `journal.md` track defects and design decisions over time, and `card` entities function as file-based task records with mandatory fields (`board/README.md`, `doc/trial/derived.md`). These are exactly the kinds of documented methods, rules, gates, and journal entries the three definitions of "the tree" point to, though the context does not explicitly state that these files collectively constitute "the tree" — that connection is implied by the recurring theme rather than asserted directly in any single cited passage.

In short, the context gives three testimonies about what the tree is — a teaching system (`journal/2026-08.md`), a conditioning workplace for memoryless sessions (`doc/notes/notes-on-the-find.md`), and a cache of past deliberation (`doc/notes/notes-on-cues.md`) — and these should be read as complementary descriptions of one system rather than competing definitions, since the context does not present them as conflicting so much as differently focused.

---
*citations: 9 resolve, 0 do not; 1 sentence(s) with no citation*

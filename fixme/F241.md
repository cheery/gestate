# F241. **[bug]** two gestate editors open at once froze the laptop, and the cause is not known

**2026-09-23, 17:56.**  A session's driven run (`Xvfb :99`, a copy of
`arc.notes`) started its editor while Henri's own was open on his
desktop, with a screen recording running.  The previous boot's journal,
in order: pipewire's ALSA sink failing from 17:56:07 (`snd_pcm_mmap_commit
error: Katkennut putki`, six times in sixteen seconds), `systemd-journald:
Under memory pressure, flushing caches` at 17:56:39, the touchpad driver
*"your system is too slow"* at 17:56:57 — and nothing after it.  The
machine was restarted at 17:57:36.

    journalctl -b -1 --no-pager | tail -15

**What is not the cause, as far as measured:** one editor's own memory.
Started alone on `:99` after the restart, the same file, sampled each
second for forty: **108 MiB** peak RSS, no child processes, 5.9 GiB of
7.6 available.  Two of those do not fill the machine, so either the
second editor behaves differently beside the first (both open the one
sound card), or the pressure came from something the journal does not
name.  Not reproduced — reproducing it means freezing his laptop again.

**What is done:** a driven run now refuses while any editor lives on
the machine, whatever display it draws on (`tools/driven.py`
`editors_running`).  The display check alone had passed, because his
window was not on `:99`.

gate: `test_driven.py::test_an_editor_open_anywhere_on_the_machine_refuses_the_run`
holds the refusal; nothing holds the freeze, whose cause is open.

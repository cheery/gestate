# F234. **[resolved]** a seek wiped a hand-played note on a bank the score also writes, so a key at the top of the timeline plays nothing

**Reported by Henri, 2026-09-16, from a DAW:** *"If I put a note onset
at the beginning of the file in the beautiful.clap, it plays nothing.
That's an old bug but checks out."*

**Measured, not read.**  Four readings of `plugin_process` gave two
theories and no evidence, which is `fixme.md` F225's morning again, so
the question went to two tests instead
(`shell/clap/src/score.rs`).  They answer it cleanly and they disagree:

* `a_seek_to_the_top_still_plays_what_stands_at_tick_zero` — **green.**
  The transport's rising edge at position zero seeks to 0 and then
  advances a block; an event *at* the target belongs to the advance, so
  both tick-0 onsets sound.  **The score path is not the defect**, and
  a note the *piece* puts at the very start is fine.
* `a_seek_erases_a_key_pressed_into_a_scored_bank` — **red**, and that
  is the defect.  A voice holding a hand-played key has its payload
  channel read back as `0` where `72` was written.

## Seen in a DAW, and counted — 2026-09-16

Henri recorded it: `~/misc/fail-2026-09-16.webm`, 8.6 s, the arrange
view with the track meters in shot.  *"On the left there's green bar
that shows when it's playing.  And the notes are displayed on the
bar."*  The meter is the oracle, so it was counted rather than watched —
frames at 4 fps, the meter's green pixels and the playhead's column per
frame:

| t (s) | playhead x | meter green px |
|---|---|---|
| 0.00–2.25 | 300 → 474 | **0** |
| 2.50 | 498 | 4086 |
| 2.75–6.75 | 522 → 882 | 3618–4671 |

And the item's note bars, measured off the same frame: note 1 spans
x 303–483, note 2 spans 480–663, note 3 660–708, note 4 705–846.

**So the meter reads exactly zero for the whole of note 1 and lights at
note 2's onset**, within one sampled frame.  The playhead runs at
91 px/s, which puts note 1 at 1.97 s long and note 2's onset at t≈2.34;
the meter's first green frame is t=2.50 and the last silent one t=2.25.

Two things that reading rules out.  It is **not** a short or clipped
envelope — a note that sounded and decayed would show *something* on a
meter for two seconds, and this shows zero. And it is **not** every
note: the second note sounds at full level immediately, which is the
same plugin, the same bank and the same voice allocator one block
later.  What is different about note 1 is only *when* it arrives: in the
block where the transport rises, which is the block where `seek` fires.

    ffmpeg -i ~/misc/fail-2026-09-16.webm -vf "fps=4,scale=1023:576:flags=neighbor" f%03d.png

## What happens

`plugin_process` does these in this order, in one block:

1. the transport's rising edge is noticed;
2. **`inst.drain(p.in_events)`** — a MIDI note-on lands in a voice and
   stamps its channels.  Draining here is deliberate and right: *"a
   listening score's question is answered with the world at its own
   instant, and a note that arrived in this block is part of that
   world"*;
3. `inst.performer.seek(...)`, because the edge rose — and `seek` puts
   **every scored bank's** voices back to `FRESH_VOICE` and every one
   of their channels back to `init_bits`.

Step 3 does not ask who wrote the voice, so it takes step 2's note with
it.  The player hears nothing.

**And the file already knew this shape**, which is the part worth
keeping.  The comment above the transport block says the order was once
the other way round and was changed for exactly this reason: *"the
first note of every play — which always shares a block with the play
edge — was applied and then wiped by the rewind."*  So the `rewind` path
was fixed by moving the drain, and the `seek` path, further down the
same function, went on doing what the rewind had stopped doing.  A
defect fixed in one branch and left standing in its neighbour.

**It is not only the top of the timeline.**  A seek fires on a rising
edge, on `needs_seek`, and on any drift past a quarter second — a loop
seam, a dragged playhead.  The same key press dies at each of them, which
is the prediction this entry stands or falls by.

**Why it needs the banks to be scored, and why that is common.**  `seek`
touches scored banks only.  A piece that writes *every* bank — which is
what a finished piece looks like — falls back to the diagonal routing,
where every bank accepts MIDI on its own channel *and* is scored.  So
the most ordinary export is the worst case.

## The shape of the fix, and the reason it is not taken here

The neighbouring code already makes exactly this distinction. When the
transport **stops**, only voices whose key is `NoteKey::Score(_)` are
released, with the reason in the comment: *the piece's notes live on the
timeline, a played key lives under the player's hands*.  `seek` should
be the same sentence in the other direction — reset what the score
wrote, leave what the hands wrote.

**It is a semantic change to a load-bearing path with a parity partner**
(`gestate/audiodynamic.py`, and `value_at` parity is what `seek`'s
docstring appeals to), so it is Henri's call and not a session's —
`doc/memory/the-slow-part-was-never-the-test.md`, *put a design choice to
him before building*.  The red test is `#[ignore]`d against this number
rather than deleted: it is the gate the moment the call is made, and
`cargo test -p gestate-clap -- --ignored` runs it today.

## Resolved — 2026-09-16, and it was his call to take

**Henri:** *"do the fix.  I think that whenever there's a note under the
playhead, it should play out."*

`seek` now clears only what the **score** wrote.  A voice holding a
`NoteKey::Midi` keeps its state and its channels, and no release is
stamped for it — which is the sentence the stop path had all along,
said in the other direction.

**The parity worry that held it back turned out not to apply**, and
that is worth recording rather than quietly dropping.  `seek`'s
docstring appeals to `value_at` parity, and Python's
`audiodynamic.Performer.seek` resets **its own allocators**, whose only
writer is the score.  One voice array with two writers is this shell's
own arrangement, documented on `VoiceState`.  So parity is a claim about
what the score does, and the score still does all of it: the replay, the
release, the replay winning a collision.  What changed is the fate of
voices Python has no word for.

**His second sentence was already true for the score half.**
`seek_into_a_held_note_resumes_it` has held since before this entry: a
seek landing inside a held note resumes it mid-envelope rather than
restarting or dropping it. What was missing was the same courtesy for a
note a hand is holding, and that is what this closes.

gate: `shell/clap/src/score.rs::a_seek_keeps_a_key_pressed_into_a_scored_bank`
and `::a_seek_clears_what_the_score_wrote_and_keeps_what_a_hand_did` —
the second exists because the skip is easy to widen by accident, and it
pins that a seek must still clear everything the score wrote.  Both were
run against the unfixed code before they were believed: with the skip
removed they both go red, and green again with it back.

## What this entry does **not** explain

Henri, the same day: *"when user is pressing a key while transport not
running, it could play the note at 'sounding' instead of being quiet."*
**No seek fires while the transport is stopped** — the whole block above
is inside `if playing` — so this entry is not the cause of that one and
it needs its own look.  Recorded here so the two are not merged by
whoever reads this next.

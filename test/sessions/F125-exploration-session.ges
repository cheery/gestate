# A gestate editor session — replay it with
#
#     python -m gestate.sessionlog <this file>
#
#: editing /home/cheery/gestate/test/sessions/lantern.ges
#
# Each line is a command and what it answered.  The
# answers are the diff: a replay that says something
# else is the report.

do
    #! playing lantern.ges at 44100 Hz — no parameters
    canvas                                 #= this file draws nothing — a canvas is a `substrate : Sig Sub` declaration
    source                                 #= source
    canvas                                 #= this file draws nothing — a canvas is a `substrate : Sig Sub` declaration
